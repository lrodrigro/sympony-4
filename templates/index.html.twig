{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <h1> {{ hello }}</h1>
    </div>
    <div class="container-fluid">
        <div class="row">
            
                <div class="col-10 mt-30">
                <button class="btn btn-primary">Bouton Color primary</button>
                <button class="btn btn-secondary">Bouton Color secondary</button>
                </div>
        </div>
    </div>
{% endblock %}